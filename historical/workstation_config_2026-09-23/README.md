# Development configuration

The original workstation configuration is preserved under:

`historical/workstation_config_2026-09-23/`

The repository does not depend on the original machine-specific `.vscode` directory.
