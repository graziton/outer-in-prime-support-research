# Archive

This directory is reserved for future repository-level distribution bundles.

Historical research artifacts that need to remain part of the provenance record are stored under `historical/`.

The authoritative research package remains under `release/`.
