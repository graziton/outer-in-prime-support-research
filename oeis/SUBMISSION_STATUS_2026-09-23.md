# OEIS Submission Status — 2026-09-23

This document records the submission state separately from the frozen 2026-09-22 release.

## A400317

The entry was re-proposed for review after editor-requested changes.

The requested terminology change to use `k` rather than `n` was applied.

## A400321

The entry was also re-proposed for review after the corresponding changes.

## References and links

During the editing process, separate REFERENCES entries were removed after the editor indicated that they were unnecessary because the relevant links were already present.

The Holt papers remain represented in the LINKS material.

## Relationship to the frozen release

The files in `release/oeis/` are historical release artifacts from 2026-09-22.

This status file records the later submission state and is intentionally kept separate so that the frozen release remains unchanged.
